"use client";

import { ReactNode } from 'react';

/**
 * Simplified AuthProvider - Zustand handles all the heavy lifting
 * This is just a placeholder for future auth-related providers
 */
export function AuthProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}