import React from 'react';

export default function TeamMemberLayout({ children }: { children: React.ReactNode }) {
  // This layout can be customized for the team-member role.
  return <>{children}</>;
}
