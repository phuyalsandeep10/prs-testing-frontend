import React from 'react';

export default function SupervisorLayout({ children }: { children: React.ReactNode }) {
  // This layout can be customized for the supervisor role.
  return <>{children}</>;
}
